{#
/**
 * @file
 *
 * Default template for wishlist templates.
 *
 * Available variables:
 * - attributes: HTML attributes for the wrapper.
 * - wishlist_template: The rendered wishlist template fields.
 *   Use 'wishlist_template' to print them all, or print a subset such as
 *   'wishlist_template.name'. Use the following code to exclude the
 *   printing of a given field:
 *   @code
 *   {{ wishlist_template|without('name') }}
 *   @endcode
 * - wishlist_template_entity: The wishlist template entity.
 * - wishlist_template_url: The wishlist template URL.
 *
 * @ingroup themeable
 */
#}
<article{{ attributes }}>
    {{ wishlist_template }}
</article>
